{
  "name": "RateLimitAttempt",
  "type": "object",
  "properties": {
    "resource_id": {
      "type": "string",
      "description": "Resource being rate-limited (serial_number, payment_intent_id, etc.)"
    },
    "resource_type": {
      "type": "string",
      "enum": [
        "bill_redemption",
        "payment_processing",
        "qr_scan"
      ],
      "description": "Type of resource"
    },
    "venue_id": {
      "type": "string",
      "description": "Venue where attempt occurred"
    },
    "actor_id": {
      "type": "string",
      "description": "User who triggered attempt"
    },
    "attempt_timestamp": {
      "type": "string",
      "format": "date-time"
    },
    "ip_address": {
      "type": "string"
    },
    "user_agent": {
      "type": "string"
    },
    "success": {
      "type": "boolean",
      "default": false
    },
    "failure_reason": {
      "type": "string"
    }
  },
  "required": [
    "resource_id",
    "resource_type",
    "attempt_timestamp"
  ]
}