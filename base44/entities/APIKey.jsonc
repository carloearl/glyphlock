{
  "name": "APIKey",
  "type": "object",
  "properties": {
    "description": {
      "type": "string",
      "maxLength": 1000
    },
    "name": {
      "type": "string",
      "description": "Key name"
    },
    "public_key": {
      "type": "string",
      "description": "Public API key"
    },
    "secret_key_hash": {
      "type": "string",
      "description": "SHA-256 hash of secret key. Plaintext secret is never persisted."
    },
    "owner_id": {
      "type": "string",
      "description": "Owner email"
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "revoked"
      ],
      "default": "active"
    },
    "permissions": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [
        "read",
        "write"
      ]
    },
    "last_used": {
      "type": "string",
      "format": "date-time"
    },
    "last_rotated": {
      "type": "string",
      "format": "date-time"
    },
    "rotation_schedule": {
      "type": "string",
      "enum": [
        "manual",
        "30_days",
        "60_days",
        "90_days"
      ],
      "default": "manual"
    },
    "geo_lock": {
      "type": "boolean",
      "default": false
    },
    "device_lock": {
      "type": "boolean",
      "default": false
    },
    "ip_allowlist": {
      "type": "string",
      "description": "Optional comma/newline separated IP/CIDR policy metadata."
    },
    "revoked_at": {
      "type": "string",
      "format": "date-time"
    },
    "revoked_by": {
      "type": "string"
    },
    "revoke_reason": {
      "type": "string"
    }
  },
  "required": [
    "name",
    "public_key",
    "secret_key_hash",
    "owner_id"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "$or": [
        {
          "data.owner_id": "{{user.email}}"
        },
        {
          "created_by_id": "{{user.id}}"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.owner_id": "{{user.email}}"
        },
        {
          "created_by_id": "{{user.id}}"
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.owner_id": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}