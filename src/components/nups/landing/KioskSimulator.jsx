import React from "react";

/**
 * The generic 30-second kiosk simulator was removed from the public NUPS
 * landing page. It did not represent the real user workflow closely enough
 * and duplicated the actual recorded contract walkthrough.
 */
export default function KioskSimulator() {
  return null;
}
